# ConfigurationManagment2

# Dependency Graph Visualizer

Инструмент для анализа и визуализации графа зависимостей пакетов Alpine Linux.  
Позволяет получать прямые и транзитивные зависимости, формировать порядок установки,
строить ASCII-деревья и визуализировать граф через Graphviz.

---

## 🚀 Возможности

- Загрузка конфигурации из YAML
- Работа с реальным репозиторием Alpine Linux (`apk`)
- Работа с тестовым репозиторием из файла
- Получение прямых и рекурсивных зависимостей (DFS)
- Обработка циклических зависимостей
- Формирование порядка установки пакета
- ASCII-дерево зависимостей
- Генерация файла Graphviz (*.dot*) + PNG визуализации
